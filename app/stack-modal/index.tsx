import { Text } from '~/components/ui/text';
import { SafeAreaView } from 'react-native-safe-area-context';

export default function StackModal() {
  return (
    <SafeAreaView className='flex-1'>
      <Text>StackModal</Text>
    </SafeAreaView>
  );
}
