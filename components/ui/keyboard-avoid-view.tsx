import { KeyboardAvoidingView } from 'react-native';
import { cssInterop } from 'nativewind';
cssInterop(KeyboardAvoidingView, { className: 'contentContainerStyle' });

export { KeyboardAvoidingView };
